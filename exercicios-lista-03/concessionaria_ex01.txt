CREATE DATABASE servico_ex02;

USE servico_ex02;

CREATE TABLE cor (
id INT,
nome_cor VARCHAR(50),

PRIMARY KEY (id)

);

CREATE TABLE marca (
id INT,
nome_marca VARCHAR(50),

PRIMARY KEY (id)

);

CREATE TABLE modelo (
id INT,
nome_modelo VARCHAR(50),

id_marca INT,

PRIMARY KEY (id),

FOREIGN KEY (id_marca)
REFERENCES marca(id)

);

CREATE TABLE proprietario (
id INT,
nome VARCHAR(50),
telefone VARCHAR(11),
email VARCHAR (50),

PRIMARY KEY (id)

);

CREATE TABLE combustivel (
id INT,
nome_combustivel VARCHAR(50),

PRIMARY KEY (id)

);

CREATE TABLE veiculo (
id INT,
ano_fabricacao INT,
ano_modelo INT,
quilometragem INT,
portas INT,

id_proprietario INT,
id_cor INT,
id_modelo INT,

PRIMARY KEY (id),

FOREIGN KEY (id_proprietario)
REFERENCES proprietario(id),

FOREIGN KEY (id_cor)
REFERENCES cor(id),

FOREIGN KEY (id_modelo)
REFERENCES modelo(id)

);

CREATE TABLE veiculo_combustivel (
id_veiculo INT,
id_combustivel INT,

PRIMARY KEY (id_veiculo, id_combustivel),

FOREIGN KEY (id_veiculo) REFERENCES veiculo (id),
FOREIGN KEY (id_combustivel) REFERENCES combustivel (id)
);

INSERT INTO cor (id, nome_cor) VALUES
(1, 'Bordô'),
(2, 'Preto'),
(3, 'Vermelho'),
(4, 'Branco');

INSERT INTO marca (id, nome_marca) VALUES
(1, 'CAOA Chery'),
(2, 'Hyundai'),
(3, 'Volkswagen');
(4, 'Fiat');

INSERT INTO modelo (id, nome_modelo, id_marca) VALUES
(1, 'Creta', 2),
(2, 'Tiggo 5X', 1),
(3, 'T-Cross', 3),
(4, 'Argo', 4);

INSERT INTO proprietario (id, nome, telefone, email) VALUES
(1, 'Anabella', 991, 'anabella@gmail.com' ),
(2, 'Matheus', 992, 'math@gmail.com'),
(3, 'Olívia', 993, 'olive@gmail.com');

INSERT INTO combustivel (id, nome_combustivel) VALUES
(1, 'Diesel'),
(2, 'Gasolina'),
(3, 'Etanol');


INSERT INTO veiculo (id, ano_fabricacao, ano_modelo, quilometragem, portas, id_proprietario, id_cor, id_modelo) VALUES
(1, 2022, 2023, 25000, 4, 1, 3, 1),
(2, 2023, 2023, 12000, 4, 2, 2, 2),
(3, 2021, 2022, 45000, 4, 3, 4, 3);

INSERT INTO veiculo_combustivel (id_veiculo, id_combustivel) VALUES
(1, 2),
(1, 3),
(2, 2),
(2, 3),
(3, 2),
(3, 3);
